this folder was named "src/" and only had one file named "Config.class"
so i renamed it to "config/" since that matches the file better

i might remove this folder and move "Config.class" it to "util/" later.

###### Signed by SleepyFish
